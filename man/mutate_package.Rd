% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutatoRpackage.R
\name{mutate_package}
\alias{mutate_package}
\title{Run Mutation Testing for an R Package}
\usage{
mutate_package(
  pkg_dir,
  cores = max(1, parallel::detectCores() - 2),
  isFullLog = FALSE,
  detectEqMutants = FALSE,
  mutation_dir = NULL,
  max_mutants = NULL,
  timeout_seconds = NULL
)
}
\arguments{
\item{pkg_dir}{Path to the package directory.}

\item{cores}{Number of parallel workers used for mutant test execution.}

\item{isFullLog}{Logical; if \code{TRUE}, prints per-mutant logs and timeout info.}

\item{detectEqMutants}{Logical; if \code{TRUE}, survived mutants are analyzed for
equivalence using the OpenAI-based workflow.}

\item{mutation_dir}{Optional directory to store generated mutant files.
If \code{NULL}, a temporary directory is used.}

\item{max_mutants}{Optional cap on the number of mutants tested.}

\item{timeout_seconds}{Optional timeout in seconds for each mutant run.
If \code{NULL}, timeout is derived from baseline runtime.}
}
\value{
An invisible list with two components:
\describe{
\item{\code{package_mutants}}{Named list with mutant path, mutation info, status,
and optional equivalence flags.}
\item{\code{test_results}}{Named list mapping mutant IDs to statuses:
\code{"KILLED"}, \code{"SURVIVED"}, or \code{"HANG"}.}
}
}
\description{
Mutates all \code{.R} files under a package's \verb{R/} directory, runs the package's
tests against each mutant in parallel, and summarizes mutation outcomes.
}
\details{
Test strategy is detected automatically:
\itemize{
\item If \verb{tests/testthat/} exists, \code{testthat::test_dir()} is used.
\item Otherwise, if \verb{tests/} exists, MutatoR installs the mutant package
with \code{--install-tests} and runs \code{tools::testInstalledPackage()}.
}
}
